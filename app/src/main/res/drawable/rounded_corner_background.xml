<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>
    <corners android:radius="12dp"/> <!-- Ajuste o valor do raio conforme necessário -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black"/> <!-- Cor e largura da borda -->
</shape>